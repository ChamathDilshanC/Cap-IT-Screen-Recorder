namespace ScreenRecorderApp.Models;

/// <summary>Marker rendered by capture. SystemDefault uses the current Windows cursor artwork.</summary>
public enum CursorStyle
{
    SystemDefault,
    Arrow,
    CircleHighlight,
    Dot,
    Crosshair,
}
