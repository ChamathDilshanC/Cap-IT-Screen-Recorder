<Page
    x:Class="ScreenRecorderApp.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctcontrols="using:CommunityToolkit.WinUI.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style x:Key="SectionDescriptionText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Padding="32,28" Spacing="18" HorizontalAlignment="Stretch">
            <StackPanel Spacing="4">
                <TextBlock Text="Settings" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="Where finished recordings are saved. Capture and audio preferences moved to their own Capture and Audio tabs."
                           Style="{StaticResource SectionDescriptionText}" />
            </StackPanel>

            <!-- Output -->
            <ctcontrols:SettingsExpander Header="Output" Description="Where finished recordings are saved" IsExpanded="True">
                <ctcontrols:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xE8B7;" />
                </ctcontrols:SettingsExpander.HeaderIcon>
                <ctcontrols:SettingsExpander.Items>
                    <ctcontrols:SettingsCard Header="Save recordings to">
                        <TextBox MinWidth="320"
                                  Text="{x:Bind ViewModel.OutputDirectory, Mode=TwoWay}"
                                  IsEnabled="{x:Bind ViewModel.IsIdle, Mode=OneWay}" />
                    </ctcontrols:SettingsCard>

                    <ctcontrols:SettingsCard Header="Output folder">
                        <Button Content="Open output folder" Command="{x:Bind ViewModel.OpenOutputFolderCommand}" />
                    </ctcontrols:SettingsCard>
                </ctcontrols:SettingsExpander.Items>
            </ctcontrols:SettingsExpander>
        </StackPanel>
    </ScrollViewer>
</Page>
